/* Action Types */
export const Types = {}

/* Reducer */
const INITIAL_STATE = {}

export default function home(state = INITIAL_STATE, action) {
  switch (action.type) {
    default:
      return state
  }
}

/* Action Creator */

export const Creators = {}
